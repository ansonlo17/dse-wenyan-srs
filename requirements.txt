# Streamlit Cloud: prefer pure binary wheels; avoid native rebuilds.
# PDF libraries optional — content ships as samples/*.txt + *.json
streamlit>=1.40,<1.51
pandas>=2.2.0,<3
numpy>=1.26.0,<3
Pillow>=10.4.0,<12
